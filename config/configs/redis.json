{
    "host": {
        "doc": "redis host",
        "format": "String",
        "default": "127.0.0.1",
        "env": "REDIS_HOST",
        "arg": "REDIS_HOST"
    },
    "port": {
        "doc": "redis port",
        "format": "Number",
        "default": 6379,
        "env": "REDIS_PORT",
        "arg": "REDIS_PORT"
    }
}