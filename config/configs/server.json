{
    "host": {
        "doc": "server host",
        "format": "String",
        "default": "127.0.0.1",
        "env": "SERVER_HOST",
        "arg": "SERVER_HOST"
    },
    "port": {
        "doc": "server port",
        "format": "Number",
        "default": 3000,
        "env": "SERVER_PORT",
        "arg": "SERVER_PORT"
    }
}