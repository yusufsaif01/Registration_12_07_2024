{
    "salt_round": {
        "doc": "encryption round",
        "format": "Number",
        "default": "10",
        "env": "SALT_ROUND",
        "arg": "salt_round"
    },
    "password" : {
        "doc": "default password",
        "format": "String",
        "default": "123123",
        "env": "PASSWORD",
        "arg": "password"
    }

}